name: Deploy Humanlike-Bot Client and Server to Staging

on:
  push:
    branches:
      - staging

permissions:
  id-token: write
  contents: read

jobs:
  # ── Run the full CI suite first ──────────────────────────────────────────────
  ci:
    uses: ./.github/workflows/ci.yml

  deploy_frontend:
    name: Deploy Frontend Humanlike-Bot Client to Staging
    runs-on: ubuntu-latest
    needs: [ci]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: |
          cd web
          npm ci

      # The API URL is baked into the bundle at build time. If the secret were
      # missing the build would still succeed and quietly ship a frontend that
      # talks to nothing, so fail loudly here instead.
      - name: Verify VITE_STAGING_API_URL is set
        env:
          VITE_API_URL: ${{ secrets.VITE_STAGING_API_URL }}
        run: |
          if [ -z "$VITE_API_URL" ]; then
            echo "::error::VITE_STAGING_API_URL is not set. Add it under Settings → Secrets and variables → Actions."
            exit 1
          fi
          echo "Building against $VITE_API_URL"

      - name: Build frontend
        env:
          VITE_API_URL: ${{ secrets.VITE_STAGING_API_URL }}
        run: |
          cd web
          npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/${{ secrets.AWS_ROLE_NAME }}
          aws-region: us-west-2

      - name: Sync S3 bucket
        run: |
          aws s3 rm s3://${{ secrets.AWS_S3_BUCKET_NAME_FRONTEND }} --recursive
          aws s3 sync web/build/ s3://${{ secrets.AWS_S3_BUCKET_NAME_FRONTEND }} --delete

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation --distribution-id ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"

  deploy_backend:
    name: Deploy Backend Humanlike-Bot Server to Staging
    runs-on: ubuntu-latest
    needs: [ci]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/${{ secrets.AWS_ROLE_NAME }}
          aws-region: us-west-2

      - name: Install AWS Elastic Beanstalk CLI
        run: |
          pip install awsebcli

      - name: Deploy Django to Elastic Beanstalk
        run: |
          cd api
          eb init -p docker ${{ secrets.AWS_BACKEND_APPLICATION_NAME }} --region us-east-1
          eb use ${{ secrets.AWS_BACKEND_ENVIRONMENT_NAME }}
          eb deploy --timeout 30
